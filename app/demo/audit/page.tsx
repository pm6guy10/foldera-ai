import { DemoAuditPage } from "@/components/demo/DemoAuditPage";

export const metadata = {
  title: "Audit Log",
  description: "Every Foldera decision is logged.",
};

export default function DemoAuditRoutePage() {
  return <DemoAuditPage />;
}
