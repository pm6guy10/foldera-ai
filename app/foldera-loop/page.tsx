import { FolderaLoopShell } from '@/components/foldera-loop/FolderaLoopShell';

export const metadata = {
  title: 'Foldera Loop',
  description: 'Deterministic Marcus estimate loop shell.',
};

export default function FolderaLoopPage() {
  return <FolderaLoopShell />;
}
