{
  "backlog_id": "between-rungs",
  "active_issue": "none",
  "base_commit": "HEAD",
  "money_loop_rung": "m2",
  "user_system_path": "Between rungs. The live product thread is the GUARDIAN (workday-presence): it fires, grounds a real commitment, and pings Slack only when it has a reviewable move. Recent shipped work: #402 (review-gated send), #404 (attachment rails), #407 (write_document delivery attachment), #411 (Sources shows the real Microsoft email), #414 (homepage continuity — brand wordmark -> home + removed the redundant final CTA). Slack delivery smoke-tested OK (manual DM via workspace). The standing CONTINUITY AUDIT is the recommended next code thread (unify the three Foldera brand marks; nav/dead-link sweep; consolidate the duplicate shells). The remaining money-loop moves are owner-gated: ALLOW_APPROVAL_EMAIL_SEND is on — confirm RESEND_API_KEY and connect a real source so a genuine move generates (brain is in safe-silence now); configure the free external 15-min guardian cron; and (paid, needs a cost ceiling) the generator companion-document build. Constraint: NO paid API calls to test — prove in the harness.",
  "allowed_file_patterns": [
    ".foldera-contract.json",
    "ACTIVE_SEAM_STATE.json",
    "ACTIVE_HANDOFF.md",
    "FOLDERA_BUILD_ORDER.yaml",
    "SESSION_HISTORY.md",
    "LESSONS_LEARNED.md",
    "docs/**"
  ],
  "forbidden_file_patterns": [
    "supabase/**",
    "app/api/**",
    "lib/**",
    "package.json",
    "package-lock.json"
  ],
  "allowed_files_raw": "Between rungs — only control-plane + docs may change until the owner names the next seam (which re-scopes this contract).",
  "forbidden_files_raw": "No product/runtime/schema/deps changes without an active seam + scoped contract.",
  "required_local_proof": "npm run gate:continuity",
  "required_browser_proof": "N/A — between rungs.",
  "browser_proof_command": "N/A",
  "acceptance_condition": "Control plane is consistent in the between-rungs form (build-order active_issue: none, contract active_issue: none, no active-seam line in ACTIVE_HANDOFF) and gate:continuity passes.",
  "is_user_facing": false,
  "anti_regression_checks": [
    "gate:continuity green in between-rungs form",
    "no active-seam line in ACTIVE_HANDOFF.md",
    "no product/runtime files changed"
  ],
  "stop_condition": "Stop when the control plane is rolled forward to a clean between-rungs state and the owner has a boot prompt for the next session.",
  "next_command": "Next code thread (recommended): the standing CONTINUITY AUDIT — unify the three brand marks, sweep nav/dead-links, consolidate the duplicate shells. OWNER-SIDE: confirm the homepage/brand fixes (#414) and the Sources MS email fix (#411); confirm RESEND_API_KEY and connect a real source so a genuine move generates, then approve to validate the live attached send; configure the free external 15-min guardian cron.",
  "terminal_state_authority": {
    "allowed": [
      "MERGED_AND_CLOSED",
      "BLOCKED_WITH_EXACT_RECEIPT",
      "HUMAN_REVIEW_REQUIRED_WITH_REASON",
      "STOPPED_WITH_AUTHORIZED_REASON"
    ]
  }
}
