# Non-Owner Beta Harness Map

This is a mock harness map only. It is meant to expose obvious first-beta blockers before asking Brandon to find a real tester. It must not be used as real beta readiness proof.

| State | Route/API | Mocked state | Expected behavior | Current coverage | Missing coverage | Likely blocker | Files involved | Proof required |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1. New unauthenticated visitor | `/`, `/start` | No session cookie | Visitor can reach the start/login path and see Google/Microsoft sign-in choices | `tests/e2e/authenticated-routes.spec.ts` has `/start` smoke | Not chained into the same non-owner beta journey | Start path drift or auth route copy mismatch | `app/start`, `tests/e2e/authenticated-routes.spec.ts` | Playwright public start smoke |
| 2. New user login/start path | `/api/auth/session`, `/onboard` | NextAuth cookie for a non-owner, non-test user with `hasOnboarded=false` | User lands in onboarding, not dashboard loops | `authenticated-routes` and `flow-routes` seed mocked sessions | No single journey asserts the seeded user is not `OWNER_USER_ID` or `TEST_USER_ID` | Harness accidentally proves Brandon/test-only behavior | `lib/auth/constants.ts`, `lib/config/constants.ts`, e2e helpers | Grep/static proof plus Playwright seeded-session proof |
| 3. User with no connected provider tokens | `/api/integrations/status`, `/onboard` | `integrations: []` | Continue and skip are disabled; Google/Microsoft connect links are visible | Existing onboarding connector-step test | Not tied to dashboard acceptance path | Onboarding could allow dashboard with no source | `app/onboard/page.tsx`, `app/api/integrations/status/route.ts` | Playwright no-token onboarding proof |
| 4. User with Google connected | `/api/integrations/status`, `/onboard`, `/dashboard?panel=sources` | Active Google token row response | Onboarding allows dashboard handoff; Sources panel shows Google connected | Existing connected checkout test and dashboard source tests | Same journey does not proceed to value loop | Connected handoff may pass but dashboard source status may drift | `app/onboard/page.tsx`, `app/dashboard/page.tsx` | Playwright connected-source handoff proof |
| 5. User with Microsoft connected | `/api/integrations/status`, `/dashboard?panel=sources` | Active Microsoft/`azure_ad` token row response | Dashboard treats Microsoft as connected and source-backed | Existing dashboard settings/source tests | Not included in first non-owner beta harness | Provider normalization drift | `app/dashboard/dashboard-page-model.tsx`, `lib/ui/provider-display.ts` | Playwright source panel proof |
| 6. Connected provider but no fresh signals | `/api/integrations/status`, `/api/source-readiness`, `/api/graph/stats`, `/api/conviction/latest`, `/api/conviction/daily-value` | Connected provider, stale/null newest signal, no latest artifact | Dashboard shows a clear waiting/freshness/source state with counts, reason, next action, and "nothing was sent" truth | First-run source-readiness unit and non-owner harness first-run state | Real beta still requires production non-owner proof | Empty first read becomes dead dashboard | `app/dashboard/page.tsx`, `app/api/source-readiness/route.ts`, dashboard state cards | Playwright waiting/no-safe-move proof with source counts |
| 7. Connected provider with fresh signals but no safe move | `/api/source-readiness`, `/api/conviction/latest`, `/api/conviction/daily-value` | Fresh source status, source counts, `daily_utility_slate.watch_item` no finished artifact | Dashboard explains that Foldera checked and held back safely | Existing daily utility slate tests and source-readiness unit tests | Real beta still requires production non-owner proof | Internal blocker terms leak or no useful waiting state | `app/dashboard/dashboard-page-model.tsx`, `app/api/source-readiness/route.ts`, `app/api/conviction/latest/route.ts` | Playwright no-safe-move state proof |
| 8. Connected provider with one source-backed move | `/api/conviction/latest`, `/api/conviction/actions/:id` | Valid discrepancy card with source refs and pending artifact/detail | Dashboard shows one current move or finished artifact, not a list | Existing dashboard action tests | No single beta harness asserts source-backed artifact after first-use setup | Latest/detail summary mismatch | `app/dashboard/page.tsx`, `app/api/conviction/latest/route.ts`, `app/api/conviction/actions/[id]/route.ts` | Playwright artifact/current-move proof |
| 9. Artifact visible with source trail | `/dashboard`, `/api/conviction/latest` | Valid `discrepancy_card`, artifact body, source refs | Artifact text and source trail render in product | Existing dashboard navigation tests | Not chained with non-owner identity and first-use flow | Discrepancy frame or source refs hidden | `app/dashboard/dashboard-page-model.tsx`, dashboard layout components | Playwright source-trail proof |
| 10. Save/skip/approve controls present | `/dashboard`, `/api/conviction/execute` | Pending document or email action | Copy, Skip, and Save/Approve controls exist and post decisions | Existing action wiring tests | Not tied to non-owner beta journey | Controls vanish for summary/detail payload | `app/dashboard/page.tsx`, `app/api/conviction/execute/route.ts` | Playwright controls + POST body proof |
| 11. Approval send disabled unless explicit send flag is enabled | `/api/conviction/execute`, dashboard status | `ALLOW_APPROVAL_EMAIL_SEND` not true | Approve records/saves but does not send email; dashboard says outbound email disabled for email artifacts | Unit and dashboard tests cover send-disabled branches | Same beta harness does not assert no outbound send request happened | Approval path could send during beta proof | `lib/conviction/execute-action.ts`, `app/dashboard/page.tsx` | Playwright network-counter proof plus focused execute tests |
| 12. Latest/history readback after save/skip | `/api/conviction/latest`, `/api/conviction/history` | Latest changes after action; history contains saved/skipped work | Dashboard reloads/reflects handled state and history has readable item | Existing history/latest route tests | Same simulated path does not verify readback after approve/skip | Successful action not visible in recent work | `app/api/conviction/latest/route.ts`, `app/api/conviction/history/route.ts`, `app/dashboard/page.tsx` | Playwright post-action latest/history proof |
| 13. OWNER_USER_ID and TEST_USER_ID excluded from non-owner proof | Acceptance gate and beta harness | Harness user differs from `OWNER_USER_ID` and `TEST_USER_ID`; production acceptance gate excludes both | Mock proof cannot count Brandon or synthetic test user as real beta proof | `acceptance-gate.ts` excludes both in `NON_OWNER_DEPTH`; beta readiness warns on owner | No focused harness/source assertion that the mock user is neither reserved ID | Mock proof becomes fake readiness | `lib/cron/acceptance-gate.ts`, `lib/ops/beta-readiness.ts`, new harness | Grep/static proof and Playwright assertion |

First missing harness/blocker to implement:

Maintain the focused Playwright harness that uses a non-owner, non-`TEST_USER_ID` identity and chains the simulated beta path across `/start`, `/onboard`, `/dashboard`, source readiness, waiting/no-safe-move, source-backed artifact, source trail, controls, send-disabled approval, skip, and history readback. This is test coverage only. It does not make real beta readiness claims.
