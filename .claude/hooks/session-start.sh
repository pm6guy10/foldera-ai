#!/usr/bin/env bash
# SessionStart hook — surfaces the execution contract into context.
# Pure read-only: never edits, never networks.

echo "=== FOLDERA ==="
echo "Contract: AGENTS.md  |  How to run/deploy: README.md  |  What it is: VISION.md"
echo "One issue, one branch, one PR. Build + lint + test must pass before a PR."
